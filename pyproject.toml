[project]
name = "memory-allocation-pygame"
version = "1.0.0"
description = "Pygame segmentation memory allocation simulator with First-Fit and Best-Fit."
requires-python = ">=3.10,<3.14"
dependencies = [
    "pygame>=2.6.0",
]
